import { Box } from '@mui/material';
import styles from './ComponentStyles.module.css'
export const SideNavBar = () => {
    return (
        <Box className={styles.SideNavBarCont}>
    
        </Box>
    )
}